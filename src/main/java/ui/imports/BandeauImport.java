package ui.imports;

import javafx.scene.control.Label;
import javafx.scene.layout.VBox;

class BandeauImport extends VBox {
    BandeauImport() {
        Label label = new Label("ICI LA BARRE D'IMPORT");

        this.getChildren().add(label);
    }
}
